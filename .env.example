# Configuracion publica del cliente movil.
# Copiar este archivo a .env.local y ajustar la URL al entorno utilizado.
# Las variables EXPO_PUBLIC_* se incluyen en la aplicacion: nunca guardar secretos aqui.

EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
