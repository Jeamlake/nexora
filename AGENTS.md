# Nexora monorepo

- The Expo application lives in `apps/mobile`.
- The NestJS application lives in `apps/api` after it is initialized.
- Run aggregate installation and checks from the repository root.

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any mobile code.
